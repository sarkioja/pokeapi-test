meta {
  name: Create Team
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/v1/teams
  body: json
  auth: none
}

headers {
  X-API-Key: {{apiKey}}
  Content-Type: application/json
}

body:json {
  {
    "name": "Dream Team",
    "trainerId": "{{trainerId}}"
  }
}
